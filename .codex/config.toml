# Project-scoped Codex config (merged when this repository is trusted).
# Use this file to turn off globally defined MCP servers only inside car-service-platform.
#
# Example — uncomment and set the server name to match your ~/.codex/config.toml:
#
# [mcp_servers.my_heavy_optional_server]
# enabled = false
#
# Docs: docs/dev/mcp-deduplication.md
# Schema: https://developers.openai.com/codex/config-schema.json
