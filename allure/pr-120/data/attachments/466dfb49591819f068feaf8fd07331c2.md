# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: staff-registers.spec.ts >> Registers workspace @mobile-only >> admin Registers mobile Units tab shows collapsible How units work
- Location: e2e/staff-registers.spec.ts:134:3

# Error details

```
Error: expect(locator).toHaveCount(expected) failed

Locator:  locator('.reference-workspace details.registers-help-disclosure')
Expected: 1
Received: 2
Timeout:  5000ms

Call log:
  - Expect "toHaveCount" with timeout 5000ms
  - waiting for locator('.reference-workspace details.registers-help-disclosure')
    9 × locator resolved to 2 elements
      - unexpected value "2"

```

# Page snapshot

```yaml
- generic [ref=e3]:
  - banner [ref=e5]:
    - button "Open workspace menu" [ref=e6] [cursor=pointer]:
      - generic [ref=e7]:
        - generic [ref=e8]:
          - strong [ref=e9]: Registers
          - generic [ref=e10]: Sections · account · sign out
        - generic [ref=e12]: ▾
  - main [ref=e13]:
    - generic [ref=e15]:
      - tablist "Registers sections" [ref=e16]:
        - tab "Units of measure" [selected] [ref=e17] [cursor=pointer]:
          - generic [ref=e18]: Units
        - tab "Services" [ref=e19] [cursor=pointer]:
          - generic [ref=e20]: Serv.
        - tab "Customers" [ref=e21] [cursor=pointer]:
          - generic [ref=e22]: Cust.
      - region "Units of measure" [ref=e24]:
        - generic [ref=e25]:
          - heading "Units of measure" [level=3] [ref=e26]
          - button "+ Add unit" [ref=e27] [cursor=pointer]
        - group [ref=e28]:
          - generic "How units work" [ref=e29] [cursor=pointer]
        - generic [ref=e31]:
          - generic [ref=e32]: Search
          - searchbox "Search units of measure" [ref=e33]
        - list "Units of measure" [ref=e34]:
          - listitem [ref=e35]:
            - button "Pieces pcs On" [ref=e36] [cursor=pointer]:
              - generic [ref=e38]:
                - generic [ref=e39]: Pieces
                - code [ref=e40]: pcs
              - generic [ref=e41]: "On"
              - img [ref=e43]
          - listitem [ref=e45]:
            - button "Liters L On" [ref=e46] [cursor=pointer]:
              - generic [ref=e48]:
                - generic [ref=e49]: Liters
                - code [ref=e50]: L
              - generic [ref=e51]: "On"
              - img [ref=e53]
          - listitem [ref=e55]:
            - button "Kilograms kg On" [ref=e56] [cursor=pointer]:
              - generic [ref=e58]:
                - generic [ref=e59]: Kilograms
                - code [ref=e60]: kg
              - generic [ref=e61]: "On"
              - img [ref=e63]
          - listitem [ref=e65]:
            - button "Meters m On" [ref=e66] [cursor=pointer]:
              - generic [ref=e68]:
                - generic [ref=e69]: Meters
                - code [ref=e70]: m
              - generic [ref=e71]: "On"
              - img [ref=e73]
          - listitem [ref=e75]:
            - button "Pairs pair On" [ref=e76] [cursor=pointer]:
              - generic [ref=e78]:
                - generic [ref=e79]: Pairs
                - code [ref=e80]: pair
              - generic [ref=e81]: "On"
              - img [ref=e83]
          - listitem [ref=e85]:
            - button "Sets set On" [ref=e86] [cursor=pointer]:
              - generic [ref=e88]:
                - generic [ref=e89]: Sets
                - code [ref=e90]: set
              - generic [ref=e91]: "On"
              - img [ref=e93]
```

# Test source

```ts
  39  |       .toBe("true");
  40  | 
  41  |     await Promise.all([
  42  |       page.waitForResponse(
  43  |         (r) => r.url().includes("/api/services") && r.request().method() === "GET" && r.status() === 200,
  44  |         { timeout: 30_000 },
  45  |       ),
  46  |       reg.openTab("Services"),
  47  |     ]);
  48  | 
  49  |     await reg.expectServicesWorkspaceVisible();
  50  |     await expect(page.locator(".services-register-page").getByText("Loading…")).toHaveCount(0, { timeout: 20_000 });
  51  | 
  52  |     await reg.servicesSearchInput().fill(E2E_DEMO_SERVICE_NAME_IN_CATALOG);
  53  |     const row = reg.serviceRowByNameSnippet(E2E_DEMO_SERVICE_NAME_IN_CATALOG);
  54  |     await expect(row).toBeVisible({ timeout: 15_000 });
  55  |     await expect(row.getByRole("group", { name: new RegExp(E2E_DEMO_SERVICE_NAME_IN_CATALOG, "i") })).toBeVisible();
  56  |   });
  57  | 
  58  |   test("admin Registers tabs do not include Invoice lines", async ({ page }) => {
  59  |     await e2eBehaviors("admin", "registers · tabs set");
  60  |     const reg = new StaffRegistersPage(page);
  61  |     await reg.gotoRegistersSection();
  62  |     await expect(page.getByRole("tab", { name: "Units of measure" })).toBeVisible();
  63  |     await expect(page.getByRole("tab", { name: "Services" })).toBeVisible();
  64  |     await expect(page.getByRole("tab", { name: "Customers" })).toBeVisible();
  65  |     await expect(page.getByRole("tab", { name: "Invoice lines" })).toHaveCount(0);
  66  |   });
  67  | 
  68  |   /** Wide viewport: help copy is not wrapped in collapsible `<details>` (mobile-only pattern). */
  69  |   test("admin Registers desktop has no collapsible help panels", async ({ page }) => {
  70  |     await e2eBehaviors("admin", "registers · desktop · no disclosure hints");
  71  |     const reg = new StaffRegistersPage(page);
  72  |     await reg.gotoRegistersSection();
  73  |     await expect(page.locator(".reference-workspace details.registers-help-disclosure")).toHaveCount(0);
  74  |   });
  75  | 
  76  |   test("admin switches to Customers and sees demo customer with vehicles", async ({ page }) => {
  77  |     await e2eBehaviors("admin", "registers · customers with vehicles");
  78  |     const reg = new StaffRegistersPage(page);
  79  |     await reg.gotoRegistersSection();
  80  |     await reg.openTab("Customers");
  81  |     await reg.expectCustomersWorkspaceVisible();
  82  | 
  83  |     await reg.expandCustomerMobileRowIfNeeded(E2E_DEMO_CUSTOMER_WITH_VEHICLES_NAME);
  84  |     const row = reg.customerRowByName(E2E_DEMO_CUSTOMER_WITH_VEHICLES_NAME);
  85  |     await expect(row).toBeVisible({ timeout: 20_000 });
  86  |     await expect(row.getByRole("button", { name: "Save" })).toBeVisible();
  87  |     await expect(row.getByRole("button", { name: "Delete" })).toBeVisible();
  88  |   });
  89  | });
  90  | 
  91  | test.describe("Registers workspace @mobile-only", () => {
  92  |   test.use({ storageState: AUTH_STATE_ADMIN });
  93  | 
  94  |   test.beforeEach(async ({ page }) => {
  95  |     await openAdminApp(page);
  96  |     await new StaffMobileNavigationPage(page).waitForStaffNavigationChrome();
  97  |     // Narrow viewport: "Registers" lives in the workspace drawer, not as a persistently visible shell button.
  98  |   });
  99  | 
  100 |   test("admin reaches Registers tabs and Services catalog on narrow viewport", async ({ page }) => {
  101 |     await e2eBehaviors("admin", "registers · mobile · services tab");
  102 |     const reg = new StaffRegistersPage(page);
  103 |     await reg.gotoRegistersSection();
  104 |     await Promise.all([
  105 |       page.waitForResponse(
  106 |         (r) => r.url().includes("/api/services") && r.request().method() === "GET" && r.status() === 200,
  107 |         { timeout: 30_000 },
  108 |       ),
  109 |       reg.openTab("Services"),
  110 |     ]);
  111 |     await reg.expectServicesWorkspaceVisible();
  112 |     await expect(reg.serviceRowByNameSnippet(E2E_DEMO_SERVICE_NAME_IN_CATALOG)).toBeVisible({ timeout: 25_000 });
  113 |   });
  114 | 
  115 |   test("admin opens Registers on Units tab and sees seeded UoM code", async ({ page }) => {
  116 |     await e2eBehaviors("admin", "registers · mobile · units of measure tab");
  117 |     const reg = new StaffRegistersPage(page);
  118 |     await reg.gotoRegistersSection();
  119 |     await reg.expectUnitsTabActive();
  120 |     await expect(reg.uomCodeCell("pcs")).toBeVisible({ timeout: 20_000 });
  121 |   });
  122 | 
  123 |   test("admin Registers tabs do not include Invoice lines on narrow viewport", async ({ page }) => {
  124 |     await e2eBehaviors("admin", "registers · mobile · tabs set");
  125 |     const reg = new StaffRegistersPage(page);
  126 |     await reg.gotoRegistersSection();
  127 |     await expect(page.getByRole("tab", { name: "Units of measure" })).toBeVisible();
  128 |     await expect(page.getByRole("tab", { name: "Services" })).toBeVisible();
  129 |     await expect(page.getByRole("tab", { name: "Customers" })).toBeVisible();
  130 |     await expect(page.getByRole("tab", { name: "Invoice lines" })).toHaveCount(0);
  131 |   });
  132 | 
  133 |   /** Narrow viewport: Units tab shows collapsible hint (`RegistersHelpDisclosure`). */
  134 |   test("admin Registers mobile Units tab shows collapsible How units work", async ({ page }) => {
  135 |     await e2eBehaviors("admin", "registers · mobile · units disclosure hint");
  136 |     const reg = new StaffRegistersPage(page);
  137 |     await reg.gotoRegistersSection();
  138 |     await reg.expectUnitsTabActive();
> 139 |     await expect(page.locator(".reference-workspace details.registers-help-disclosure")).toHaveCount(1);
      |                                                                                          ^ Error: expect(locator).toHaveCount(expected) failed
  140 |     await expect(
  141 |       page.locator("details.registers-help-disclosure summary").filter({ hasText: "How units work" }),
  142 |     ).toBeVisible();
  143 |   });
  144 | 
  145 |   test("admin switches to Customers and sees demo customer with vehicles", async ({ page }) => {
  146 |     await e2eBehaviors("admin", "registers · mobile · customers with vehicles");
  147 |     const reg = new StaffRegistersPage(page);
  148 |     await reg.gotoRegistersSection();
  149 |     await reg.openTab("Customers");
  150 |     await reg.expectCustomersWorkspaceVisible();
  151 | 
  152 |     await reg.expandCustomerMobileRowIfNeeded(E2E_DEMO_CUSTOMER_WITH_VEHICLES_NAME);
  153 |     const row = reg.customerRowByName(E2E_DEMO_CUSTOMER_WITH_VEHICLES_NAME);
  154 |     await expect(row).toBeVisible({ timeout: 20_000 });
  155 |     await expect(row.getByRole("button", { name: "Save" })).toBeVisible();
  156 |     await expect(row.getByRole("button", { name: "Delete" })).toBeVisible();
  157 |   });
  158 | });
  159 | 
```