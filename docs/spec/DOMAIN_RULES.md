# DOMAIN_RULES

Этот файл является source of truth для критичной доменной логики `car-service-platform`.

- Last updated: `2026-04-09`
- Status: `repair operations + pdf-backed analytics baseline`

## 1) Core Entities
Текущий baseline сущностей:
- `Customer`
- `Vehicle`
- `RepairOrder`
- `RepairWork`
- `RepairPart`
- `Supplier`
- `RepairPhoto`
- `CompletionAct`
- `RepairDocument`
- `RepairFinancialSnapshot`

Для каждой сущности фиксировать:
- цель
- обязательные поля
- владельца source of truth
- допустимые статусы

### Initial Entity Notes
- Один `Customer` может иметь несколько `Vehicle`.
- Один `Vehicle` принадлежит одному активному `Customer` в рамках первой версии.
- `Vehicle` должен по возможности опираться на управляемый справочник марки, модели и года.
- Удаление `Customer`, у которого уже есть `Vehicle`, должно блокироваться до явного удаления или переназначения автомобилей.
- Один `RepairOrder` относится к одному `Vehicle` и одному `Customer`.
- Создание `RepairOrder` через QuickFocus / VPR flow может инициировать inline-создание `Vehicle` и `Customer`, но итоговая связь все равно должна быть `RepairOrder -> Vehicle -> Customer` без обходных или временных сущностей.
- Один `RepairOrder` может содержать несколько `RepairWork`, `RepairPart` и `RepairPhoto`.
- Каждый `RepairOrder` должен иметь отдельный публичный `tracking code` формата `TOR-*` для клиента.
- Каждый `RepairOrder` должен иметь назначенного `master`, отвечающего за текущую работу.
- `CompletionAct` формируется на основании завершенного `RepairOrder`.
- `RepairDocument` хранит выгруженный PDF и его метаданные.
- `RepairFinancialSnapshot` хранит versioned финансовое состояние ремонта на момент выгрузки документа.

## 2) Lifecycle Rules
Базовый lifecycle ремонта для текущего prototype baseline:
- `new -> in_progress -> waiting_parts -> completed`

Правила:
- `new`: обращение создано, клиент, автомобиль и исходное описание проблемы уже зафиксированы.
- `in_progress`: мастер ведет ремонт, добавляет notes, фото и связанные операционные данные.
- `waiting_parts`: ремонт приостановлен в ожидании деталей или внешней поставки.
- `completed`: ремонт завершен, итог готов к выдаче и формированию акта.

Дополнительно:
- допустимые рабочие переходы baseline: `new -> in_progress`, `new -> waiting_parts`, `in_progress -> waiting_parts`, `waiting_parts -> in_progress`, `in_progress -> completed`
- перевод из `completed` назад не должен происходить без явного административного override
- `CompletionAct` может быть сформирован только для `completed`
- `RepairDocument` и `RepairFinancialSnapshot` могут создаваться только для `completed`, если не включен явный административный override
- каждая новая выгрузка PDF должна создавать новую snapshot-версию, а предыдущие версии должны оставаться неизменяемыми
- service board calendar должен визуализировать актуальное operational состояние ремонта от текущей даты и не должен вводить в заблуждение относительно статуса
- moneyflow dashboard по умолчанию должен открываться на rolling-периоде последних 30 дней, считая от текущего дня
- staff surface должен быть vehicle-centric: машина и её история доступны, customer identity и контакты скрыты
- месячная история должна строиться по данным `RepairOrder` и связанных сущностей, а не из отдельных ручных отчетов
- `issue note` является исходным описанием проблемы и не должна переписываться после создания без отдельного административного override
- `repair notes` должны вестись как append-only история с автором и временем
- удаление `repair note` допустимо только автором этой заметки
- каждое изменение статуса должно фиксировать автора и время изменения

## 3) Pricing Rules
Базовые правила первой версии:
- итог ремонта = сумма `RepairWork.cost` + сумма `RepairPart.cost`
- суммы по работам и запчастям считаются на backend как source of truth
- для `RepairPart` нужно различать закупочную стоимость и цену продажи клиенту
- сумма по запчастям для клиента считается от цены продажи, а не от закупочной стоимости
- UI может показывать предварительный расчет, но не должен быть источником итоговой суммы
- financial snapshot для аналитики должен хранить минимум:
  - сумму работ
  - сумму запчастей для клиента
  - сумму закупок
  - сумму прочих расходов/дополнительных позиций, если они есть в PDF
  - итоговую сумму документа
  - версию snapshot и timestamp выгрузки
- historical analytics должна строиться по сохраненным `RepairFinancialSnapshot`, а не пересчитываться только из текущего состояния `RepairOrder`
- PDF, показанный в аналитике, и snapshot, используемый для подсчетов, должны ссылаться на одну и ту же выгрузку
- если появятся скидки, налоги, fees и округления, они должны быть описаны здесь до реализации

### 3.1) Dashboard analytics: snapshot и даты
Правила для staff Operations Dashboard (MoneyFlow / Billing / ServiceBoard):

- **Официальные суммы «по акту (PDF)» для завершённых работ в периоде:** для каждого `RepairOrder` со `status=completed` и `completed_at` в выбранном диапазоне дат берётся **последняя** версия `RepairDocument` (максимальный `version`, при равенстве — больший `id`) и связанный с ней **ровно один** `RepairFinancialSnapshot`. Суммы полей snapshot (`labor_total`, `parts_client_total`, `parts_purchase_total`, `other_expenses_total`, `document_total`) агрегируются по множеству таких ремонтов. Ремонты без ни одного `RepairDocument` в эту сумму **не** входят (но учитываются в KPI покрытия).
- **Ряд по дням «выгрузки актов»:** для графика активности экспорта в периоде по оси времени используется дата `RepairFinancialSnapshot.created_at` (календарный день в часовом поясе сервера); **каждая** строка snapshot (каждая выгрузка PDF) учитывается отдельно — повторный экспорт того же ремонта даёт дополнительный вклад в день выгрузки.
- **Живая оценка MoneyFlow** (расчёт на клиенте из текущих ремонтов и закупок без snapshot) остаётся **предпросмотром** до выгрузки акта; источник цен услуг для этой оценки — справочник `Service.price` с API, без произвольных захардкоженных fallback-цен.
- **KPI покрытия:** число ремонтов со `completed_at` в периоде без единого `RepairDocument` фиксируется отдельно как «закрыто в системе, акт не сформирован».
- **Операционные метрики ServiceBoard** (cycle time, воронка по статусам): считаются по ремонтам, у которых дата `created_at` попадает в выбранный для вкладки диапазон (тот же фильтр, что и для списка ремонтов на ServiceBoard), если в UI не указано иное.
- **Блок MoneyFlow API (`moneyflow`):** топ поставщиков по сумме `purchase_price * quantity` за период (фильтр `Purchase.order_date`); **несвязанные закупки** — строки без привязки к ремонту (`repair_code` пустой или NULL) в том же периоде; **экспорты по сотруднику** — число `RepairDocument` с датой `created_at` в периоде, группировка по `exported_by` (без пользователя — отдельная строка).
- **Расходники мастерской (`Purchase.is_shop_consumable`):** строки закупки с флагом **не входят** в строки completion PDF / в расчёт `RepairFinancialSnapshot.parts_*` и **не участвуют** в агрегатах блока **Warehouse** (склад/портфель поставщиков) и в **MoneyFlow**-метриках «топ поставщиков» / «несвязанные закупки»; отдельно в `moneyflow` отдаётся сводка **`shop_consumables`** (количество строк и buy total за период по `order_date`). Количество измеряется в выбранной **единице измерения** (`UnitOfMeasure`, расширяемый справочник). Привязка к ремонту через `repair_code` допускается для операционного учёта, но **не** переводит такую строку в акт.
- **Справочник единиц измерения (`UnitOfMeasure`):** в API список для staff по умолчанию только с `is_active=true`; параметр `include_inactive=true` и операции **POST/PATCH/DELETE** на `/api/purchases/units/` — только у пользователя с `is_staff` (роль **admin** в продукте). В CRM на экране **Purchases** у админа есть кнопка **Units of measure**: добавление строк, правка названия/порядка/флага active, удаление (если на единицу есть закупки — ошибка; нужно снять **Active**).

## 4) Access Rules
До появления полноценной auth-модели использовать минимальный baseline ролей:
- `owner`: полный доступ и видимость по всей системе
- `admin`: работа с клиентами, автомобилями, ремонтами, закупками и актами
- `master`: обновление назначенных ремонтов, фото, notes и связанных рабочих данных
- `client`: доступ только к просмотру статуса ремонта и связанных материалов по `tracking code`, без отдельного кабинета

Критичные ограничения:
- закрытие ремонта и формирование акта должны быть ограничены ролями `owner` и `admin`
- прямое изменение итоговых сумм вне source-of-truth логики запрещено
- после статуса `completed` запись не должна возвращаться в активный поток без отдельного override-механизма
- historical analytics не должна терять прошлые значения после повторной выгрузки или ручного изменения ремонта
- клиент не должен попадать в staff surface и не должен иметь доступ к Django Admin
- публичный клиентский доступ не должен использовать последовательный ID ремонта как единственный идентификатор
- staff updates по ремонту должны фиксировать, кто именно добавил note или изменил статус
- inline-создание `Customer` и `Vehicle` из QuickFocus flow должно уважать те же access checks и ownership rules, что и основные `/customers` и `/vehicles` сценарии
- закрытие доступа `staff` должно выполняться только `admin`-пользователем и не должно разрушать исторические записи, где сотрудник участвовал как автор или исполнитель
- легенда service board calendar должна однозначно объяснять, какие визуальные обозначения соответствуют каким repair statuses
- default диапазон moneyflow не должен зависеть от границ данных в БД, если требование продукта — всегда показывать `today - 30 days` -> `today`
- customer PII не должна попадать в staff vehicle views, serializers и related APIs

## 5) Integration Rules
На старте проекта внешние интеграции не входят в обязательный scope.

Если интеграции будут добавлены:
- инициатор и источник истины должны быть описаны явно
- retry, timeout и обработка частичных сбоев должны фиксироваться здесь до реализации

## 6) Confirmed Product Rules From Source Document
- система должна поддерживать глобальный поиск по номеру машины и имени клиента
- номер автомобиля должен быть уникальным в реестре первой версии
- карточка автомобиля должна показывать историю работ по машине
- карточка клиента должна показывать список автомобилей и историю обращений
- flow создания нового VPR не должен требовать предварительного перехода в отдельный реестр для создания обязательных сущностей
- внутри ремонта должны храниться фото до, во время и после ремонта
- фото должны поддерживать мобильную съемку, дозагрузку новых файлов и удаление ошибочных кадров
- внутри ремонта должны храниться работы, запчасти и поставщики
- для заказанных запчастей должны храниться дата заказа, закупочная цена, количество, сумма закупки, цена продажи клиенту и привязка к ремонту / автомобилю
- закупка запчастей может существовать без привязки к ремонту и автомобилю как складская / резервная закупка
- месячная история должна поддерживать агрегаты по суммам и количеству объектов
- акт выполненных работ должен содержать клиента, автомобиль, таблицу работ, таблицу запчастей и итоговую сумму
- PDF выгрузки для завершенного ремонта должен содержать как минимум работы, запчасти, итоговые суммы и все дополнительные финансовые позиции, влияющие на аналитику
- аналитический dashboard должен учитывать именно те цены, которые сохранены в выгруженном PDF / связанном snapshot
- admin должен иметь возможность закрыть доступ `staff`-аккаунту на users screen без прямого редактирования базы вручную
- service board должен помогать видеть, что происходит "сейчас": активные работы, ожидание деталей и другие живые операционные состояния
- moneyflow должен начинаться с предсказуемого периода последних 30 дней, чтобы все пользователи входили в одинаковый финансовый контекст
- staff должен иметь доступ ко всем машинам и их repair history, если это не раскрывает customer identity

## 7) Open Decisions
- хранить ли `email` клиента в первой версии
- может ли один `RepairOrder` содержать несколько отдельных проблем как структурированные элементы
- нужен ли fallback в ручной ввод, если нужной машины нет в справочнике
- нужен ли учет оплат клиента
- нужен ли учет сотрудников, выполняющих конкретные работы
- нужен ли склад запчастей
- ~~можно ли иметь несколько PDF/snapshot-версий на один `RepairOrder` и как выбирать активную версию по умолчанию~~ **закрыто для dashboard:** допускается несколько версий; для итогов «по последнему акту» используется последняя версия по полю `version` (см. §3.1); ряд по дням экспорта учитывает все выгрузки
- нужны ли ручные корректировки snapshot после выгрузки
- как моделировать "прочие расходы" и дополнительные финансовые строки
- увольнение staff: нужно ли хранить деактивированных пользователей в системе для истории или достаточно физического удаления
- service board calendar: какой период считать основным по умолчанию и какие даты использовать как source of truth для размещения ремонта на шкале
- разрешать ли пользователю менять moneyflow dates вручную после открытия, если начальное состояние всегда переинициализируется на последних 30 днях
- нужен ли отдельный role-aware serializer/endpoint для staff vehicles, чтобы гарантированно не утекали customer identity fields
- нужен ли VIN-enrichment после MVP и какой провайдер будет использоваться
- нужны ли уведомления о ТО и запланированном ремонте

## 8) Review Policy
`domain-reviewer` обязателен, если меняются:
- бизнес-правила и domain workflow
- статусы и переходы
- расчеты и totals
- eligibility, ограничения и доменные constraints
- права доступа
- терминология предметной области и правила согласованности данных
- критичные доменные инварианты
