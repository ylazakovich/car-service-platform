/**
 * Default-branch Test Report still uses `--config ./allurerc.mjs` (see
 * workflow_run + default-branch workflow rules). Checkout is the PR head.
 * Canonical Allure Report 3 config lives in `scripts/allure/allurerc.mjs`.
 */
export { default } from "./scripts/allure/allurerc.mjs";
